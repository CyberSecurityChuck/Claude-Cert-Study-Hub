AI-generated question sets are auto-saved here as .js files and are
auto-discovered by the dashboard when it loads. You do NOT need to
edit manifest.js -- just generate a set and it shows up automatically.
